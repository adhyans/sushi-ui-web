import { Meta } from '@storybook/addon-docs';
import { SecondaryColorsContainer, PrimaryColorsContainer } from './story';

<Meta title='Colors' component={PrimaryColorsContainer} />

```js
import red from 'sushiweb/core/tokens/color/red';
```

```js
// Color variants
const redColor = red.z500;
const lightRedColor = red.z100;

// colors without variants
const blackColor = black;
const whiteColor = white;

// Use with a component
<Button btnColor={red.z500} />;
```

<PrimaryColorsContainer />
<SecondaryColorsContainer />
