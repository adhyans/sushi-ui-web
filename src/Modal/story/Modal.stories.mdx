import { Meta } from '@storybook/addon-docs';
import Modal from '../Modal';
import { ModalStory } from './story';

<Meta title='Modal' component={Modal} />

```jsx
import Modal from 'sushiweb/core/Modal';
```

<ModalStory />

```jsx
<Modal
  visible={isVisible}
  onClose={handleClose}
  type='medium'
  title='Modal Title'
  titleComponent={<h2>Hey There</h2>}
  showCloseIcon={false}
  slideOnMobile
  isCentered
  padding='2rem'
  preventFocus
>
  <div> This is a simple Modal component demo</div>
</Modal>
```
